var fs = require('fs');

module.exports = function (grunt) {
    grunt.registerTask('my-place-install', 'MyPlace installer', function () {
        grunt.log.writeln('Actually MyPlace installer does nothing');
    });
};